{
  "private": true,
  "scripts": {
    "development": "mix",
    "watch": "mix watch",
    "watch-poll": "mix watch -- --watch-options-poll=1000",
    "hot": "mix watch --hot",
    "production": "mix --production",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "laravel-mix": "^6.0.6",
    "lint-staged": "^13.2.3",
    "resolve-url-loader": "^4.0.0",
    "sass": "^1.34.1",
    "sass-loader": "^12.2.0",
    "webpack": "^5.50.0",
    "webpack-cli": "^4.7.2"
  },
  "dependencies": {
    "js-cookie": "^3.0.5"
  },
  "lint-staged": {
    "*.php": "scripts/fix-phpcs.sh"
  }
}
